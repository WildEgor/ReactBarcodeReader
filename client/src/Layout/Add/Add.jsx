import React, { Fragment } from 'react';
import AddItem from '../../components/AddItem/AddItem';

const Add = props => {
  return (
    <Fragment>
      <AddItem />
    </Fragment>
  );
};

export default Add;